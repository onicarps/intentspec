{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"IntentSpec","text":"<p>Coverage and enforcement layer for AI agent infrastructure.</p> <p>IntentSpec transforms agent development from ad-hoc documentation to versioned, testable, and enforceable intent. Document agent behavior as code, measure intent coverage, score intent debt, and enforce intent through CI/CD.</p>"},{"location":"#quickstart","title":"Quickstart","text":"<pre><code>pip install intentspec\nintentspec init --quickstart\nintentspec validate\n</code></pre>"},{"location":"#features","title":"Features","text":"<ul> <li>Validate \u2014 Schema + semantic validation of intent.yaml</li> <li>Score \u2014 Intent Debt Score (IDS 0-100) with 7 weighted components</li> <li>Coverage \u2014 Structural coverage analysis (tools, goals, constraints, non-negotiables)</li> <li>Diff \u2014 Git-integrated intent change tracking</li> <li>Lint \u2014 Quality checks for agent specs</li> <li>CI \u2014 Unified CI/CD hook with exit codes</li> <li>Audit Report \u2014 SOC 2 / EU AI Act compliance documents</li> <li>Convert \u2014 Import from AGENTS.md, SKILL.md, agentskills, CrewAI</li> <li>Templates \u2014 5 built-in agent templates</li> </ul>"},{"location":"#supported-input-formats","title":"Supported Input Formats","text":"Format Command AGENTS.md <code>intentspec init --from AGENTS.md &lt;path&gt;</code> SKILL.md <code>intentspec init --from SKILL.md &lt;path&gt;</code> agentskills <code>intentspec init --from agentskills &lt;path&gt;</code> CrewAI <code>intentspec init --from crewai.yaml &lt;path&gt;</code> Templates <code>intentspec init --template &lt;name&gt;</code>"},{"location":"#license","title":"License","text":"<p>MIT</p>"},{"location":"adapters/","title":"Adapters","text":"<p>IntentSpec can import agent specs from multiple formats.</p>"},{"location":"adapters/#crewai","title":"CrewAI","text":"<p>Parse a CrewAI <code>crewai.yaml</code> config into intent.yaml:</p> <pre><code>intentspec init --from crewai.yaml ./crewai.yaml\n</code></pre>"},{"location":"adapters/#mapping","title":"Mapping","text":"CrewAI Field intent.yaml Field <code>agents[].role</code> <code>agent.name</code> (kebab-case) <code>agents[].backstory</code> <code>agent.description</code> <code>agents[].tools</code> <code>tools.allowed</code> <code>agents[].allow_delegation</code> <code>boundaries</code> <code>tasks[].goal</code> <code>intent.goals[].description</code> <code>tasks[].description</code> <code>intent.goals[].description</code> (fallback)"},{"location":"adapters/#example-crewaiyaml","title":"Example crewai.yaml","text":"<pre><code>agents:\n  - role: \"Researcher\"\n    backstory: \"An expert researcher who finds and synthesizes information.\"\n    allow_delegation: false\n    tools:\n      - \"web_search\"\n      - \"file_reader\"\n\ntasks:\n  - description: \"Research the topic\"\n    goal: \"Find and synthesize relevant information\"\n</code></pre>"},{"location":"adapters/#agentsmd","title":"AGENTS.md","text":"<p>Parse an AGENTS.md file (Markdown with agent specification):</p> <pre><code>intentspec init --from AGENTS.md ./AGENTS.md\n</code></pre>"},{"location":"adapters/#skillmd","title":"SKILL.md","text":"<p>Parse a SKILL.md file (YAML frontmatter + Markdown body):</p> <pre><code>intentspec init --from SKILL.md ./SKILL.md\n</code></pre>"},{"location":"adapters/#agentskills","title":"agentskills","text":"<p>Parse an agentskills directory (SKILL.md + Resources/Scripts/References):</p> <pre><code>intentspec init --from agentskills ./my-skill/\n</code></pre>"},{"location":"ci-integration/","title":"CI Integration Guide","text":"<p>This guide explains how to wire <code>intentspec ci</code> into any CI/CD system. The command is designed as a drop-in CI gate: it aggregates validation, linting, scoring, and coverage checks into a single exit code, so your pipeline can simply run the command and let the exit code decide pass or fail.</p>"},{"location":"ci-integration/#exit-codes","title":"Exit Codes","text":"<p><code>intentspec ci</code> returns one of four exit codes. Your CI job's pass/fail status is driven entirely by this code.</p> Code Meaning Trigger 0 Pass No errors, no warnings, coverage meets threshold 1 Validation error Schema or semantic errors, lint errors; under <code>--strict</code> also any warning 2 Warning Warnings only (semantic/lint), non-strict mode 3 Fatal Spec file missing, empty, or unreadable; coverage below <code>--min-coverage</code> threshold; malformed <code>--config</code> <p>Aggregation across multiple files uses rank order (highest wins): fatal (3) &gt; error (1) &gt; warning (2) &gt; pass (0).</p>"},{"location":"ci-integration/#cli-flags","title":"CLI Flags","text":"<pre><code>intentspec ci [OPTIONS] [PATHS]...\n</code></pre> Flag Description <code>--min-coverage N</code> Minimum coverage threshold (0-100). Exit 3 if computed coverage is below N. <code>--strict</code> Promote warnings to errors. Exit 2 never occurs under <code>--strict</code>. <code>--config PATH</code> Path to a <code>.intentspec.yaml</code> config file. Overrides auto-discovered config. <code>--format FORMAT</code> Output format: <code>text</code> (default), <code>json</code>, or <code>yaml</code>. <p><code>PATHS</code> are files or directories to check. Directories are globbed for <code>**/intent.yaml</code>. Defaults to <code>.</code> when no path is given.</p>"},{"location":"ci-integration/#reproducing-exit-codes","title":"Reproducing Exit Codes","text":"<p>Each exit code can be reproduced from the command line:</p> <p>Exit 0 \u2014 valid spec: <pre><code>intentspec ci tests/fixtures/valid_intent.yaml\necho $?  # 0\n</code></pre></p> <p>Exit 1 \u2014 schema-invalid spec: <pre><code>intentspec ci tests/fixtures/invalid_intent.yaml\necho $?  # 1\n</code></pre></p> <p>Exit 2 \u2014 warning-only spec (non-strict): <pre><code># A spec with a short agent description triggers a lint warning\ncat &gt; /tmp/warning_spec.yaml &lt;&lt; 'EOF'\nversion: \"1.0\"\nagent:\n  name: \"short-bot\"\n  type: \"coding\"\n  description: \"Short\"\nintent:\n  goals:\n    - description: \"A short goal\"\n      priority: \"medium\"\n  constraints:\n    - rule: \"Always be helpful\"\n      enforceable: true\n  non_negotiables:\n    - rule: \"Never harm users\"\n      severity: \"hard\"\n  tools:\n    allowed:\n      - name: \"cli\"\n        rationale: \"Command line access\"\n    denied: []\nEOF\nintentspec ci /tmp/warning_spec.yaml\necho $?  # 2\n</code></pre></p> <p>Exit 3 \u2014 missing spec: <pre><code>intentspec ci /nonexistent/path.yaml\necho $?  # 3\n</code></pre></p> <p>Exit 3 \u2014 coverage below threshold: <pre><code>intentspec ci tests/fixtures/valid_intent.yaml --min-coverage 100\necho $?  # 3\n</code></pre></p>"},{"location":"ci-integration/#usage-snippets","title":"Usage Snippets","text":""},{"location":"ci-integration/#jenkins","title":"Jenkins","text":"<pre><code>pipeline {\n  agent any\n\n  stages {\n    stage('Intent Spec') {\n      steps {\n        sh 'pip install intentspec'\n        sh 'intentspec ci --min-coverage 50 --strict'\n      }\n    }\n  }\n\n  post {\n    success {\n      echo 'IntentSpec CI: all checks passed (exit 0)'\n    }\n    failure {\n      echo 'IntentSpec CI: checks failed (exit 1, 2, or 3)'\n    }\n  }\n}\n</code></pre> <p>Jenkins treats any non-zero exit code as a build failure by default. The <code>sh</code> step propagates the exit code directly, so the stage fails when <code>intentspec ci</code> returns 1, 2, or 3.</p> <p>If you want to distinguish warnings (exit 2) from hard failures (exit 1 or 3), use a scripted approach:</p> <pre><code>pipeline {\n  agent any\n\n  stages {\n    stage('Intent Spec') {\n      steps {\n        sh 'pip install intentspec'\n        script {\n          def exitCode = sh script: 'intentspec ci --min-coverage 50; echo \\$?', returnStdout: true\n          def code = exitCode.trim().toInteger()\n          if (code == 3) {\n            error 'IntentSpec CI: fatal \u2014 missing spec or below coverage threshold'\n          } else if (code == 1) {\n            error 'IntentSpec CI: validation errors detected'\n          } else if (code == 2) {\n            echo 'IntentSpec CI: warnings detected (exit 2)'\n            // Mark build as unstable instead of failed\n            currentBuild.result = 'UNSTABLE'\n          }\n        }\n      }\n    }\n  }\n}\n</code></pre>"},{"location":"ci-integration/#circleci","title":"CircleCI","text":"<pre><code>version: 2.1\n\njobs:\n  intentspec-ci:\n    docker:\n      - image: python:3.11\n    steps:\n      - checkout\n\n      - run:\n          name: Install intentspec\n          command: pip install intentspec\n\n      - run:\n          name: Run intentspec ci\n          command: intentspec ci --min-coverage 50 --strict\n</code></pre> <p>CircleCI fails the job when any <code>run</code> step exits non-zero, so the <code>intentspec ci</code> exit code drives pass/fail directly. To treat exit code 2 (warning) as a soft failure, add a step that inspects the code:</p> <pre><code>version: 2.1\n\njobs:\n  intentspec-ci:\n    docker:\n      - image: python:3.11\n    steps:\n      - checkout\n\n      - run:\n          name: Install intentspec\n          command: pip install intentspec\n\n      - run:\n          name: Run intentspec ci (allow warnings)\n          command: |\n            intentspec ci --min-coverage 50\n            exit_code=$?\n            if [ $exit_code -eq 3 ]; then\n              echo \"FATAL: missing spec or below coverage threshold\"\n              exit 1\n            elif [ $exit_code -eq 1 ]; then\n              echo \"ERROR: validation errors\"\n              exit 1\n            elif [ $exit_code -eq 2 ]; then\n              echo \"WARNING: warnings detected\"\n              exit 0\n            fi\n</code></pre>"},{"location":"ci-integration/#azure-devops","title":"Azure DevOps","text":"<pre><code>trigger:\n  - main\n\npool:\n  vmImage: ubuntu-latest\n\nsteps:\n  - script: pip install intentspec\n    displayName: Install intentspec\n\n  - script: intentspec ci --min-coverage 50 --strict\n    displayName: Run intentspec ci\n</code></pre> <p>Azure Pipelines fails the task when <code>script</code> steps exit non-zero, so the <code>intentspec ci</code> exit code governs the task result directly. To differentiate exit code 2 (warning) from hard failures:</p> <pre><code>trigger:\n  - main\n\npool:\n  vmImage: ubuntu-latest\n\nsteps:\n  - script: pip install intentspec\n    displayName: Install intentspec\n\n  - script: |\n      intentspec ci --min-coverage 50\n      exit_code=$?\n      if [ $exit_code -eq 3 ]; then\n        echo \"##vso[task.logissue type=error]FATAL: missing spec or below coverage threshold\"\n        exit 1\n      elif [ $exit_code -eq 1 ]; then\n        echo \"##vso[task.logissue type=error]Validation errors detected\"\n        exit 1\n      elif [ $exit_code -eq 2 ]; then\n        echo \"##vso[task.logissue type=warning]Warnings detected (exit 2)\"\n        exit 0\n      fi\n    displayName: Run intentspec ci\n</code></pre> <p>The <code>##vso[task.logissue]</code> logging command surfaces warnings and errors in the Azure DevOps build timeline.</p>"},{"location":"ci-integration/#config-file","title":"Config File","text":"<p><code>intentspec ci</code> reads <code>.intentspec.yaml</code> from the working directory automatically, or an explicit file via <code>--config PATH</code>. Supported keys:</p> <pre><code>min_coverage: 50    # equivalent to --min-coverage 50\nstrict: true        # equivalent to --strict\nformat: json        # equivalent to --format json\n</code></pre> <p>Precedence (highest wins): CLI flag &gt; environment variable &gt; config file &gt; built-in default.</p> <p>Environment variables: <code>INTENTSPEC_MIN_COVERAGE</code>, <code>INTENTSPEC_STRICT</code>, <code>INTENTSPEC_FORMAT</code>.</p>"},{"location":"ci-integration/#combining-with-other-integrations","title":"Combining with Other Integrations","text":"<p>For GitHub Actions, see the composite action in <code>action/action.yml</code> and the example workflow in <code>.github/workflows/intentspec.yml</code>. For GitLab CI, see <code>.gitlab-ci.yml</code>. For pre-commit, see <code>.pre-commit-hooks.yaml</code>.</p>"},{"location":"commands/","title":"Commands","text":""},{"location":"commands/#validate","title":"validate","text":"<p>Validate intent.yaml against schema and semantic rules.</p> <pre><code>intentspec validate [PATH] [--strict] [--format text|json|yaml]\n</code></pre> <p>Exit codes: - <code>0</code> \u2014 Valid - <code>1</code> \u2014 Schema validation error - <code>2</code> \u2014 Semantic warning (use <code>--strict</code> to fail on warnings)</p>"},{"location":"commands/#init","title":"init","text":"<p>Initialize intent.yaml from an existing spec, template, or interactive wizard.</p> <pre><code># From existing spec\nintentspec init --from AGENTS.md &lt;path&gt;\nintentspec init --from SKILL.md &lt;path&gt;\nintentspec init --from agentskills &lt;path&gt;\nintentspec init --from crewai.yaml &lt;path&gt;\n\n# From template\nintentspec init --template coding-agent\nintentspec init --template data-pipeline --name my-pipeline\nintentspec init --template list\n\n# Interactive wizard\nintentspec init --quickstart\n</code></pre>"},{"location":"commands/#score","title":"score","text":"<p>Calculate Intent Debt Score (IDS 0-100).</p> <pre><code>intentspec score [PATH] [--by-agent] [--weights JSON] [--format text|json|yaml]\n</code></pre> <p>The IDS is calculated from 7 weighted components:</p> Component Weight Tool coverage 25% Goal coverage 15% Constraint coverage 10% Non-negotiable coverage 15% Freshness 10% Completeness 15% Consistency 10%"},{"location":"commands/#coverage","title":"coverage","text":"<p>Show intent coverage percentage.</p> <pre><code>intentspec coverage [PATH] [--format text|json|yaml]\n</code></pre>"},{"location":"commands/#diff","title":"diff","text":"<p>Show intent changes between commits.</p> <pre><code>intentspec diff [PATH] [--semantic] [--source-commit HASH] [--format text|json|yaml]\n</code></pre>"},{"location":"commands/#lint","title":"lint","text":"<p>Quality checks for intent specs.</p> <pre><code>intentspec lint [PATH] [--format text|json|yaml]\n</code></pre> <p>Checks: goal description length, constraint enforceability, tool rationale, non-negotiable severity, duplicate tools, agent description.</p>"},{"location":"commands/#ci","title":"ci","text":"<p>CI/CD hook \u2014 runs validate + lint + score + coverage in one pass.</p> <pre><code>intentspec ci [PATH] [--min-coverage N] [--strict] [--format text|json|yaml]\n</code></pre> <p>Exit codes: - <code>0</code> \u2014 All checks pass - <code>1</code> \u2014 Validation error - <code>2</code> \u2014 Warning (stale/sparse) - <code>3</code> \u2014 Below threshold or missing spec</p>"},{"location":"commands/#audit-report","title":"audit-report","text":"<p>Generate a compliance audit report.</p> <pre><code>intentspec audit-report [PATH] [--format text|json|yaml]\n</code></pre> <p>Produces a SOC 2 / EU AI Act compliance document with agent inventory, full spec dump, IDS score, and SHA-256 hash.</p>"},{"location":"examples/","title":"Examples","text":""},{"location":"examples/#minimal-intentyaml","title":"Minimal intent.yaml","text":"<pre><code>version: \"1.0\"\nagent:\n  name: \"my-agent\"\n  type: \"custom\"\n  description: \"A simple agent\"\nintent: {}\n</code></pre>"},{"location":"examples/#coding-agent","title":"Coding Agent","text":"<pre><code>version: \"1.0\"\nagent:\n  name: \"code-reviewer\"\n  type: \"coding\"\n  description: \"Reviews PRs for code quality and security\"\nintent:\n  goals:\n    - description: \"Identify bugs and security vulnerabilities\"\n      priority: \"high\"\n    - description: \"Suggest improvements with clear rationale\"\n      priority: \"medium\"\n  non_negotiables:\n    - rule: \"Never approve code with hardcoded secrets\"\n      severity: \"hard\"\n  tools:\n    allowed:\n      - name: \"github_api\"\n        rationale: \"Required for PR review\"\n    denied:\n      - name: \"production_deployer\"\n        rationale: \"Deployment requires human approval\"\n</code></pre>"},{"location":"examples/#using-templates","title":"Using Templates","text":"<pre><code># List available templates\nintentspec init --template list\n\n# Create from template\nintentspec init --template data-pipeline --name my-etl\n\n# Create from CrewAI config\nintentspec init --from crewai.yaml ./crewai.yaml\n</code></pre>"},{"location":"examples/#cicd-integration","title":"CI/CD Integration","text":"<pre><code># Run all checks\nintentspec ci --min-coverage 80 --strict\n\n# In GitHub Actions\n- uses: onicarps/intentspec-action@v1\n  with:\n    min-coverage: 80\n    strict: true\n</code></pre>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.11+</li> <li>pip</li> </ul>"},{"location":"installation/#install-from-pypi","title":"Install from PyPI","text":"<pre><code>pip install intentspec\n</code></pre>"},{"location":"installation/#install-from-source","title":"Install from source","text":"<pre><code>git clone https://github.com/onicarps/intentspec.git\ncd intentspec\npip install -e \".[dev]\"\n</code></pre>"},{"location":"installation/#verify","title":"Verify","text":"<pre><code>intentspec --version\n# IntentSpec 0.1.0\n</code></pre>"},{"location":"installation/#quickstart","title":"Quickstart","text":"<p>Initialize a new intent spec interactively:</p> <pre><code>intentspec init --quickstart\n</code></pre> <p>Or from an existing AGENTS.md:</p> <pre><code>intentspec init --from AGENTS.md ./AGENTS.md\n</code></pre> <p>Validate your spec:</p> <pre><code>intentspec validate\n</code></pre> <p>Check the Intent Debt Score:</p> <pre><code>intentspec score\n</code></pre>"},{"location":"schema/","title":"Schema Reference","text":""},{"location":"schema/#intentyaml-v10","title":"intent.yaml v1.0","text":"<pre><code>version: \"1.0\"\n\nagent:\n  name: string                    # Required. kebab-case identifier.\n  type: enum                       # Required. coding | research | service | data | coordinator | custom\n  description: string              # Required. \u2264200 chars.\n\nintent:\n  goals:                           # Optional.\n    - description: string          # Required. What the agent achieves.\n      priority: enum               # high | medium | low\n      success_criteria: string     # Optional. How to measure this goal.\n\n  constraints:                     # Optional.\n    - rule: string                 # Required. The rule text.\n      enforceable: boolean         # Required. true = auto-checkable, false = human judgment.\n\n  non_negotiables:                 # Optional. Hard boundaries.\n    - rule: string                 # Required.\n      severity: enum               # hard = CI fail, soft = CI warning.\n\n  tools:                           # Optional.\n    allowed:                       # Tools the agent CAN use.\n      - name: string               # Required.\n        rationale: string          # Required. WHY this tool is needed.\n    denied:                        # Tools the agent CANNOT use.\n      - name: string               # Required.\n        rationale: string          # Required. WHY this tool is forbidden.\n\n  boundaries:                      # Optional.\n    - scope: string                # What's in scope.\n      out_of_scope: string         # What's explicitly out.\n\n  escalation:                      # Optional.\n    trigger: string                # When to escalate.\n    method: string                 # How (human review, supervisor agent, etc.).\n\n  failure_modes:                   # Optional. Known ways this agent can fail.\n    - mode: string                 # Required.\n      mitigation: string           # Required.\n\nmetadata:\n  status: enum                     # draft | active | deprecated\n  owner: string                    # Team or individual responsible.\n  created: string                  # ISO-8601 date.\n  updated: string                  # ISO-8601 date.\n  review_cycle: string             # e.g., \"monthly\"\n  tags: [string]                   # Free-form tags.\n</code></pre>"},{"location":"schema/#agent-types","title":"Agent Types","text":"Type Description <code>coding</code> Code review and development tasks <code>research</code> Information gathering and analysis <code>service</code> User-facing service delivery <code>data</code> Data pipelines and ETL <code>coordinator</code> Multi-agent orchestration <code>custom</code> User-defined agent type"},{"location":"schema/#severity-levels","title":"Severity Levels","text":"Level Behavior <code>hard</code> CI/CD pipeline fails <code>soft</code> CI/CD pipeline warns"}]}