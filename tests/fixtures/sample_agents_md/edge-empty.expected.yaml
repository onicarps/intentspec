# Ground truth for benchmark — do not auto-generate
# Empty source file — converter should emit a minimal valid Intent
# with agent.name from the filename stem and a placeholder description.
version: "1.0"

agent:
  name: edge-empty
  type: custom
  description: "<empty source>"

intent: {}
